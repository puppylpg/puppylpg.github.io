---
name: ai-trends-report
description: "抓取并分析最新 AI/LLM 趋势网页的全部动态图表，将逐图中文解读生成 Jekyll 博客文章和站内图片，并执行本地预览、用户确认及后续发布流程。用户要求总结 AI 趋势、生成 AI 趋势博客或分析 llm-stats.com/ai-trends 时使用。"
---

# Claude Code 兼容入口

这是代理 skill，不是真实维护源。

真实 skill 位于：

```text
.agents/skills/ai-trends-report/SKILL.md
```

使用本 skill 时，必须读取并遵循上述真实文件；真实文件引用的 `scripts/` 和 `references/` 资源也从 `.agents/skills/ai-trends-report/` 读取。
